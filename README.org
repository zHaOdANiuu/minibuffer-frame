* minibuffer-frame

Display the minibuffer in a child frame centered relative to the parent frame.

** Requirements

- Emacs 28.1 or newer
- A graphical Emacs session (GUI frame)

Windows Terminal usually runs Emacs in a terminal frame. Terminal frames do not support the child frames, pixel-based positioning, or graphical focus handling required by this package, so the package may not work correctly there. On Windows, use a graphical Emacs session instead of terminal Emacs in Windows Terminal.

** Installation

#+begin_src emacs-lisp
(use-package minibuffer-frame
  :if (display-graphic-p)
  :vc (:url "https://github.com/zHaOdANiuu/minibuffer-frame" :rev :newest)
  :hook (window-setup . minibuffer-frame-mode))
#+end_src

If Emacs always runs in a graphical environment, such as a graphical Linux or macOS session, you can remove =:if (display-graphic-p)=:

** Configuration

Use =M-x customize-group RET minibuffer-frame RET=, or set the variables directly:

| Variable               | Default | Description                               |
|------------------------+---------+-------------------------------------------|
| =minibuffer-frame-width= |     0.5 | Child frame width as a fraction of parent |
| =minibuffer-frame-top=   |     0.3 | Top offset as a fraction of parent        |

The number of completion lines is controlled by Emacs's built-in =completions-max-height= variable. When it is unset, the frame displays one line.

** Demo

[[file:example/a.gif]]

** How it works

- Creates and centers the child frame when the minibuffer is first activated.
- Resizes the frame according to the completion candidates.
- Reuses the same frame for recursive minibuffer sessions.
- Hides the frame after the outermost minibuffer exits.
- Restores focus while the minibuffer is active and supports returning after =other-window=.

** Development

#+begin_src sh
make all
#+end_src

You can also run =make lint=, =make byte-compile=, or =make run= separately.
